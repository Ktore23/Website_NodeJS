<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chi tiết nhà trọ - <%= room.name %></title>
    
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/detail.css"> <!-- Nếu có CSS tùy chỉnh -->
</head>
<body class="bg-light">
    <div class="container">
        <h1 class="text-center mb-4"><%= room.name %></h1>
        <img class="detail-image img-fluid" src="/<%= room.image %>" alt="<%= room.name %>">
        
        <ul class="list-group mb-4">
            <li class="list-group-item"><strong>Giá:</strong> <%= room.price.toLocaleString('vi-VN') %> VNĐ</li>
            <li class="list-group-item"><strong>Mô tả:</strong> <%= room.description %></li>
            <li class="list-group-item"><strong>Địa chỉ:</strong> <%= room.address %></li>
        </ul>

        <a href="/" class="btn btn-secondary">← Quay lại danh sách</a>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
